/**
 * printing the value of EOF.
*/

#include <stdio.h>

int main(int argc, char const *argv[])
{
    int value_of_EOF = EOF;         //value of EOF is -1 as defined in the stdio.h as a macro.
    printf("%d", value_of_EOF);
    return 0;
}
